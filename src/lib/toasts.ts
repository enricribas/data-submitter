import type { ToastSettings } from "@skeletonlabs/skeleton";

export const saved: ToastSettings = {
	message: "Saved successfully",
};
export const deleted: ToastSettings = {
	message: "Deleted successfully",
};
